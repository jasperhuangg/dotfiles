**Test:** `ClassName::testMethodName`

**File:** `script/manual/path/to/TestFile.php`

This test failed in AZ CI and appears unrelated to the changes in my PR. Creating this issue so the failure is tracked and suppressed for unrelated PRs going forward.

_**If you are the assignee, please fix this with urgency, as it points to an existing failure in the system that is likely affecting customers.**_

**Link(s) to Failing PR(s):**
- [<repo> #<PR_NUMBER>](https://github.com/Expensify/<repo>/pull/<PR_NUMBER>)

**CI Run:**
- [<description>](https://github.com/Expensify/<repo>/actions/runs/<RUN_ID>/job/<JOB_ID>)

**Error summary:**
```
Paste the relevant failure output here (assertion message + file:line stack)
```

**Why this is unrelated to my PR:**
Brief, specific explanation of why this failure is not caused by your changes (which files/features your
PR touches vs. what the failing test exercises, and that your own tests passed).
